{
  "name": "root",
  "private": true,
  "scripts": {
    "lerna": "lerna",
    "build": "lerna run build --scope=@realitychain/{nft-parcels,erc20,sdk,nft-vouchers}",
    "prepare": "lerna run prepare --scope=@realitychain/{nft-parcels,erc20,sdk,nft-vouchers}",
    "lint": "lerna run --parallel lint --scope=@realitychain/{nft-parcels,erc20,sdk,nft-vouchers}",
    "format": "lerna run --parallel format",
    "start": "yarn workspace @realitychain/webapp start",
    "test": "lerna run --parallel test --scope=@realitychain/{nft-parcels,erc20,nft-vouchers}"
  },
  "resolutions": {
    "**/ethers": "^5.6.8",
    "**/@ethersproject/contracts": "^5.6.2"
  },
  "devDependencies": {
    "@types/jest": "^28.1.4",
    "hardhat": "^2.9.6",
    "husky": "^8.0.1",
    "lerna": "^5.1.4",
    "ts-node": "^10.8.2"
  },
  "workspaces": [
    "packages/*",
    "packages/realitychain-contracts/*"
  ],
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{ts,js}": [
      "yarn lint",
      "yarn format"
    ]
  }
}
